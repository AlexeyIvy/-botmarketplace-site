// @botmarketplace/shared
// Shared types, constants, and utilities for the monorepo.
// Will be populated as features are implemented.

export const API_PREFIX = "/api" as const;
export const API_V1_PREFIX = "/api/v1" as const;
