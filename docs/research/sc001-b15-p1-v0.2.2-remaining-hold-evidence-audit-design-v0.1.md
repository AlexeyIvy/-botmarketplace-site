# SC001 — B15-P1 v0.2.2 Remaining Hold Evidence Audit Design v0.1

Date: 2026-09-24  
Parent state: `CHECKPOINT_BACKUP_RESTORE_VERIFY_PASS`  
Source run: `20260920T210446Z`

## Objective

Resolve, if and only if official identity/network evidence is sufficient, the nine remaining v0.2.1 unresolved rows:

- GRAM / Bybit TON
- GRAM / OKX The Open Network (TON)
- QTUM / Bybit QTUM
- QTUM / OKX Quantum
- STX / Bybit STX
- STX / OKX l-Stacks
- XLM / Bybit XLM / Stellar Lumens
- USDT / Bybit CORN
- USDT / OKX Tempo

This is a non-price identity audit. It must not use price, PnL, current transfer ON/OFF state, trading outcomes, fuzzy matching or rescue tuning.

## Evidence policy

A candidate resolution requires:

1. the exact frozen venue row;
2. official project, venue, token-system or network evidence establishing the underlying identity;
3. an exact deterministic mapping or row-specific normalization;
4. no automatic route creation from one-sided evidence;
5. no modification of v0.2.1 frozen artifacts in place.

The audit produces only v0.2.2 candidate actions.

## Candidate actions under review

### GRAM

Evidence indicates Toncoin was rebranded from TON to Gram/GRAM in June 2026, while remaining the native token of The Open Network.

Candidate rule:

- preserve canonical network `ton:mainnet`;
- add GRAM as an exact native-asset alias for the post-rebrand identity;
- resolve both frozen GRAM/TON empty-contract rows to `native:GRAM`;
- do not rewrite historical TON identity.

### QTUM

Frozen venue rows are already asset-scoped to QTUM:
- Bybit: chain code/type `QTUM`;
- OKX: exact alias `Quantum`.

Official Qtum evidence establishes QTUM as the native transferable asset of Qtum mainnet.

Candidate rule:

- add `qtum:mainnet`;
- Bybit exact code/type: `QTUM` / `QTUM`;
- OKX exact alias: `Quantum`;
- native asset: `QTUM`;
- no fuzzy alias matching.

### STX

Frozen venue rows are asset-scoped to STX:
- Bybit: `STX` / `Stacks`;
- OKX: exact alias `l-Stacks`.

Official Stacks documentation establishes STX as the native token of Stacks mainnet. A Bybit network-upgrade announcement independently refers to deposits/withdrawals via Stacks (STX).

Candidate rule:

- add `stacks:mainnet`;
- exact venue aliases only;
- native asset: `STX`.

### XLM

Official Stellar documentation establishes XLM as the native asset of Stellar and states native XLM has no issuer representation. OKX also describes XLM as Stellar's native token.

Frozen rows:
- Bybit: chain code `XLM`, chainType `Stellar Lumens`, empty contract;
- OKX: alias `Stellar Lumens`, mainNet=true, raw contract marker `native:GDN4U3BDFVMK74IJ46GXEN5ZDRA5QIJQF6TM3NBJIG4IQW2RDGYXSMRN`.

Candidate rule:

- add exact Bybit chainType variant `Stellar Lumens`;
- add one exact OKX row-specific override for the frozen marker above;
- output identity `native:XLM`;
- no generic handling of arbitrary `native:` prefixes.

If the exact row-specific evidence is considered insufficient, XLM must remain review.

### USDT / CORN

Frozen Bybit row:
- asset: USDT
- chain: CORN
- contract: `0xB8CE59FC3717ada4C02eaDF9682A9e934F625ebb`.

Evidence identifies Corn as a real network carrying USDT0 and an explorer labels this exact contract as USDT0 on Corn.

Candidate disposition:

- add token-only network identity `corn:mainnet`;
- exact Bybit alias/type `CORN`;
- contract canonicalization: lowercase exact EVM address;
- classify representation as `KNOWN_ONE_SIDED`;
- no cross-venue route admission.

### USDT / Tempo

Frozen OKX row:
- asset: USDT
- alias: Tempo
- contract: `0x20c00000000000000000000014f22ca97301eb73`.

USDT0 and Tempo evidence identify this exact address as USDT0 on Tempo.

Candidate disposition:

- add `tempo:mainnet`;
- exact OKX alias `Tempo`;
- exact address identity, represented as the canonical lowercase address;
- classify as `KNOWN_ONE_SIDED`;
- no cross-venue route admission.

## Expected candidate-only dry-run if all evidence gates pass

Starting v0.2.1:
- closed old-review assets = 42
- remaining review assets = 4
- unresolved rows = 9
- asset common representations = 48
- USDT common representations = 12

Candidate v0.2.2:
- closed old-review assets = 46
- remaining review assets = 0
- unresolved rows = 0
- asset common representations = 52
- USDT common representations = 12
- CORN and Tempo = known one-sided only
- new cross-venue quote routes from CORN/Tempo = 0

Composite candidate:
- admitted assets = 192
- review assets = 0
- excluded assets = 9
- total assets = 201
- canonical representations = 207
- directed asset identity edges = 414

These are acceptance targets for the candidate audit only. They may not be changed after the audit result is observed.

## Stop rules

Any evidence mismatch, ambiguous network identity, unexpected source row, route leak, alias collision, or special-handler conflict => STOP.

No terminal zero-review freeze PASS may be declared until:
1. v0.2.2 evidence audit passes;
2. exact patch/preflight passes;
3. exact replay passes;
4. a separate freeze preflight passes.
