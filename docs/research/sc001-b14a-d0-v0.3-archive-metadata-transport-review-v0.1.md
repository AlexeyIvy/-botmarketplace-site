# SC001 — B14-A D0 v0.3 Archive Metadata Transport Review v0.1

Date: 2026-09-19
Status: **SOURCE-TRANSPORT REVIEW / PRODUCT SEMANTICS PASS / NO PRICE OUTCOME**
Scope: `SCALPING RESEARCH / SC001`

## 1. Observed v0.3 state

Exact token:

`B14A_D0_V03_SOURCE_ARCHIVE_METADATA_REVIEW`

Observed:

- future BTC-USD expiry contracts = 6;
- future ETH-USD expiry contracts = 6;
- BTC-USD-SWAP hedge identity = PASS;
- ETH-USD-SWAP hedge identity = PASS;
- FUTURES archive metadata pass counts = 0 / 0;
- all price/basis/settlePx/deliveryPx/convergence/PnL firewalls = false.

## 2. What passed

The current supported product branch is real and source-qualified at instrument level:

- six future BTC-USD standard crypto-margined expiry contracts;
- six future ETH-USD standard crypto-margined expiry contracts;
- same-venue inverse perpetual hedge identities exist.

Therefore B14-A is not blocked by product availability.

## 3. Root cause hypothesis

The v0.3 archive probe used the C11-specific historical resolver:

`/priapi/v5/broker/public/trade-data/download-link`

with:

- module = 1;
- instType = FUTURES;
- instFamilyList = BTC-USD / ETH-USD.

However SC001's earlier qualified official historical-data probe used:

`GET /api/v5/public/market-data-history`

with:

- module;
- instType;
- dateAggrType = 1D;
- begin/end;
- exact `instIdList`.

For dated FUTURES, each expiry is a distinct instrument ID. Family-only archive resolution was never independently qualified.

Classification:

`B14A_FUTURES_ARCHIVE_METADATA_TRANSPORT_NOT_YET_QUALIFIED`

This is a source-transport REVIEW, not a strategy or data-availability reject.

## 4. Required v0.4 correction

Use the previously qualified public endpoint:

`GET /api/v5/public/market-data-history`

For every probe-eligible expiry contract pass:

- module = 1;
- instType = FUTURES;
- dateAggrType = 1D;
- begin = probe-day UTC start;
- end = next-day UTC start;
- instIdList = exact expiry `instId`.

Recursively inspect response metadata for exact trusted archive identity.

No archive body GET/open.

## 5. No-change list

Unchanged:

- BTC-USD / ETH-USD inverse expiry product scope;
- >=2 future contracts per family;
- inverse BTC-USD-SWAP / ETH-USD-SWAP hedges;
- >=1 metadata/HEAD pass per family;
- yesterday deterministic probe date;
- all price/outcome firewalls.
