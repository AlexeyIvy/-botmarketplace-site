# SC001 — B13-A v0.1 Structural Preflight Implementation-Fail Review v0.1

Date: 2026-09-19
Status: **ENGINEERING SOURCE-PARSER REVIEW / NO STRUCTURAL VERDICT**
Scope: `SCALPING RESEARCH / SC001`

Parent:
`docs/research/sc001-current-roadmap-and-stop-rules-v4.84.md`

## 1. Observed state

The frozen B13-A v0.1 run stopped at the first symbol:

`BTC`

with:

`B13A_STRUCTURAL_PREFLIGHT_IMPLEMENTATION_FAIL`

and:

`RuntimeError: OKX funding metadata details missing for BTC`

## 2. Interpretation

This is not:

- `B13A_DEFER_SOURCE_OR_SAMPLE`;
- `B13A_REJECT_STRUCTURAL`;
- `B13A_STRUCTURAL_PREFLIGHT_SURVIVE`.

No funding differential statistic or structural gate was computed.

No market price, basis, trade/L2 body or strategy PnL was accessed.

## 3. Root cause

The v0.1 B13-A parser expected a specific OKX historical-data JSON shape:

`data.details -> groupDetails`

However the already-qualified C9-D0 v0.2 implementation does not assume that wrapper. It recursively walks all nodes under `data` and admits exact file nodes exposing:

- filename/fileName;
- url/fileUrl/downloadUrl.

This approach already produced:

`C9_D0_V02_DATA_SEMANTICS_PASS`

on the same OKX historical funding service.

Classification:

`ENGINEERING_OKX_METADATA_RESPONSE_SHAPE_PARSER_DEFECT`

## 4. Allowed repair

Create B13-A v0.2 changing only the OKX historical archive metadata parser:

- recursively walk `obj["data"]`;
- accept exact file nodes only;
- retain trusted-host and exact-basename checks;
- retain monthly module-3 funding source;
- retain H1-2025 chronology.

All financial/statistical rules stay unchanged.

## 5. No-change list

Do not change:

- 12-symbol universe;
- OKX/Bybit venues;
- H1-2025 window;
- 5-minute matching tolerance;
- 40 bps structural burden;
- 50 bps qualifying differential;
- sample/breadth/concentration gates;
- price/PnL firewalls.

## 6. Rerun

After v0.2 implementation freeze, rerun all 12 symbols from the beginning.

No candidate ID is assigned by this repair.
