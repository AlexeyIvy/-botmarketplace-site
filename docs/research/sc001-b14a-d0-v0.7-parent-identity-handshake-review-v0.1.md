# SC001 — B14-A D0 v0.7 Parent-Identity Handshake Review v0.1

Date: 2026-09-19
Status: **ENGINEERING HANDSHAKE REVIEW / NO SOURCE OR PRICE OUTCOME**
Scope: `SCALPING RESEARCH / SC001`

## 1. Observed state

B14-A D0 v0.7 self-test returned:

`B14A_D0_V07_SELF_TEST_REVIEW`

Error:

`freeze identity mismatch: source_review_git_blob_sha`

## 2. Root cause

The v0.7 runner was derived from v0.6 and still checked:

- `source_review_git_blob_sha`;
- `selector_review_git_blob_sha`.

The v0.7 freeze was rebuilt around the newer archive-semantics parent:

- `semantics_review_git_blob_sha`.

Therefore runner and freeze did not share the same parent-identity schema.

Classification:

`ENGINEERING_PARENT_CHAIN_HANDSHAKE_MISMATCH`

## 3. Research implication

The self-test stopped before any OKX API call.

Therefore:

- no historical metadata request was issued by this run;
- no archive URL was followed;
- no HEAD was issued;
- no body was opened;
- no price/basis/settlePx/deliveryPx/convergence/PnL was accessed.

## 4. Allowed repair

Create v0.8 with one explicit parent chain frozen in both runner and freeze:

1. product-family source review;
2. FUTURES selector / T+2 availability review;
3. futureschain archive semantics review;
4. current protocol;
5. runner identity.

No source logic, product scope, archive filename semantics, thresholds, or firewalls change.
