# SC001 — C8-D1 v0.1 Historical Clock Integrity Review v0.1

Date: 2026-09-18
Status: **REVIEW — KNOWN OKX UTC-DAY ARCHIVE BOUNDARY MISMATCH / NOT C8 RESEARCH FAIL**

Exact terminal state:

`C8_D1_HISTORICAL_CLOCK_INTEGRITY_REVIEW`

Observed:

- synchronization golden tests: `C8_D1_SYNC_GOLDEN_PASS`;
- OKX parsed rows: `1,761,145`;
- OKX 1s usable-grid share: about `0.6615`;
- OKX p99 staleness: `1567 ms`;
- Bybit parsed rows: `2,049,441`;
- Bybit 1s usable-grid share: about `0.9757`;
- Bybit p99 staleness: `1752.2 ms`;
- joint usable-grid share: about `0.6430`;
- failed gate includes `okx_zero_out_of_day`;
- no cross-venue price/return/dislocation/lag was calculated;
- no signal/PnL/promotional alpha was calculated.

## Root cause identified

SC001 Q006R already established that historical OKX daily trade archives are aligned to a 16:00 UTC boundary (UTC+8 archive convention).

A single archive label D is not a full UTC calendar day.

Frozen qualified reconstruction rule from Q006R:

`archive D + archive D+1 -> retain created_time in UTC [D 00:00, D+1 00:00)`

C8-D1 v0.1 incorrectly tried to qualify the UTC day from archive D alone.

Therefore the OKX out-of-day and coverage failure is an implementation/data-boundary issue.

## Research integrity

This REVIEW is not C8 mechanism evidence.

No synchronization thresholds are changed in the repair.

D1 v0.2 must first apply the already-qualified Q006R UTC-stitch rule, then rerun the same frozen:

- 1-second grid;
- 2-second staleness;
- per-venue 0.98 usable-share gate;
- joint 0.95 usable-share gate;
- p99 staleness <=1000 ms;
- max adjacent gap <=5 seconds.

Only after that repaired measurement may any further clock-design decision be considered.
