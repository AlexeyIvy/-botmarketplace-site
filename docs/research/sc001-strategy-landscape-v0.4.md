# SC001 — Strategy Landscape v0.4

Date: 2026-09-18
Status: **CURRENT COVERAGE MAP AFTER C8B-S0 / NON-ALPHA GOVERNANCE ARTIFACT**
Supersedes: `sc001-strategy-landscape-v0.3.md`

## 1. Inheritance

All terminal/coverage conclusions from v0.3 remain binding.

C1-C6 remain terminal `REJECT_SENTINEL`.

C9-S1 remains terminal `C9_S1_REJECT_SENTINEL`.

## 2. C8B — paired cross-venue transient relative-basis convergence

Frozen structural sentinel:

`C8B-S0 cross-venue relative-basis structural headroom`

Terminal state:

`C8B_S0_REJECT_HEADROOM`

Observed on the frozen 2025-01-20 Selection/Calibration day:

- strict coactive seconds: 82,024;
- baseline-eligible seconds: 81,904;
- baseline-eligible hours: 24;
- persistent >=30 bps same-sign two-second episodes: 0;
- p99 absolute relative-basis dislocation: about 2.4455 bps;
- maximum absolute dislocation: about 20.8001 bps.

Coverage implication:

- the strict same-second OKX/Bybit BTC perpetual paired-convergence mechanism is now covered with decisive negative structural-headroom evidence;
- synchronized data quality was ample;
- the rejection occurred before convergence, quote, legging or PnL modeling because raw dislocation scale itself was insufficient.

No threshold/persistence/baseline rescue is authorized.

C8A directional lead/lag remains a conceptually separate mechanism and may not be activated as a rescue of C8B.

## 3. Reusable knowledge retained

C8 contributes reusable market/data blocks:

- strict coactive 1-second no-carry-forward synchronization;
- cross-venue raw basis as R6/R2;
- causal local cross-venue basis median as R6;
- transient relative-basis deviation as a measured but economically weak R1/R2 state for paired convergence.

See:

- `sc001-feature-evidence-registry-v0.4.md`;
- `sc001-reusable-market-building-blocks-registry-v0.3.md`.

## 4. Remaining next-slate gaps

After C9 and C8B:

- C10 L2 liquidity-vacuum/replenishment mechanism independent of C5;
- C7 spread-qualified non-BTC/multi-asset maker/hybrid universe.

Under-covered means information gap only, not expected profitability.

## 5. Immediate next direction

Proceed to:

`C10 — L2 LIQUIDITY-VACUUM / REPLENISHMENT`

First step must be no-alpha/local-data preflight on already contaminated BTC OKX L2 sources.

Do not:

- condition on C5 aggressive-flow events;
- reuse C5 labels;
- choose a book threshold from price outcomes;
- run fill/queue/PnL simulation in the first C10 stage.
