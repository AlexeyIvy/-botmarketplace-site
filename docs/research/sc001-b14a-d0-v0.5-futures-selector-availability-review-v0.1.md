# SC001 — B14-A D0 v0.5 FUTURES Selector / Availability Review v0.1

Date: 2026-09-19
Status: **SOURCE-TRANSPORT SEMANTIC REVIEW / NO PRICE OUTCOME**
Scope: `SCALPING RESEARCH / SC001`

## 1. Observed state

B14-A D0 v0.5 returned:

`B14A_D0_V05_SOURCE_ARCHIVE_METADATA_REVIEW`

Product semantics and hedge identities remain valid.

All price/basis/settlePx/deliveryPx/convergence/PnL firewalls remained false.

## 2. Additional API-semantic findings

The historical market-data endpoint has instrument-selector semantics by product type.

For `FUTURES/SWAP/OPTION` the historical query is family-oriented and requires:

`instFamilyList`

rather than the SPOT-style:

`instIdList`.

The exact expiry contract must therefore be selected from returned family metadata by exact archive filename.

## 3. Availability lag

Historical modules 1/2/3/11 are normally available on T+2.

Therefore a probe using yesterday's date can be too fresh and create a false source REVIEW unrelated to actual archive support.

## 4. Corrected source-feasibility probe

v0.6 must:

- use `instFamilyList=BTC-USD` or `ETH-USD`;
- keep `instType=FUTURES`;
- use `dateAggrType=daily`;
- choose a deterministic completed probe date = current UTC date minus 3 days;
- query probe label-day metadata window, then previous-day metadata window;
- accept only exact `INSTID-trades-PROBE_DATE.zip`;
- HEAD exact trusted archive only;
- never GET/open the body.

## 5. Representative contract rule

For source-transport qualification only, select one deterministic representative per family:

`earliest-expiring currently live eligible contract whose listTime is before the probe date`.

This is not price/outcome selection.

No headroom or return is inspected.

## 6. No-change list

Unchanged:

- BTC-USD / ETH-USD inverse standard expiry futures;
- same-venue inverse SWAP hedges;
- no price/basis/settlePx/deliveryPx/convergence/PnL;
- no candidate ID.
