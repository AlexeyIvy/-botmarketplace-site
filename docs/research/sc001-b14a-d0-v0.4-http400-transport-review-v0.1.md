# SC001 — B14-A D0 v0.4 HTTP-400 Transport Review v0.1

Date: 2026-09-19
Status: **ENGINEERING SOURCE-TRANSPORT REVIEW / NO PRICE OUTCOME**
Scope: `SCALPING RESEARCH / SC001`

## 1. Observed state

B14-A D0 v0.4 returned:

`B14A_D0_V04_SOURCE_ARCHIVE_METADATA_REVIEW`

Product semantics passed:

- BTC-USD future contracts = 6;
- ETH-USD future contracts = 6;
- BTC-USD-SWAP hedge = PASS;
- ETH-USD-SWAP hedge = PASS.

Archive metadata probe failed for every eligible contract with:

`HTTP Error 400: Bad Request`

All price/basis/settlePx/deliveryPx/convergence/PnL firewalls remained false.

## 2. Root cause class

The failure occurs before archive resolution and HEAD.

Therefore this is:

`B14A_HISTORICAL_ENDPOINT_REQUEST_SEMANTICS_REVIEW`

not:

- archive absence;
- product absence;
- strategy reject.

## 3. Qualified SC001 reference implementation

Previously successful SC001 historical metadata preflights use the public endpoint:

`GET /api/v5/public/market-data-history`

with:

- module = `1`;
- exact instType;
- `dateAggrType = daily`;
- exact UTC day begin/end;
- exact `instIdList`.

Examples already qualified in SC001 include C8-D0 and E006.

Those implementations also allow searching both:

- the archive label-day metadata window;
- the previous-day metadata window;

while still requiring one exact trusted filename.

## 4. v0.4 divergence

B14-A v0.4 used:

`dateAggrType = 1D`

The live endpoint returned HTTP 400.

The next implementation must use the already-qualified SC001 live transport exactly:

`dateAggrType = daily`

with exact `instIdList`.

## 5. v0.5 allowed repair

Change only historical archive metadata transport:

- dateAggrType -> `daily`;
- probe exact label-day window first;
- if unresolved, probe previous-day metadata window;
- require exact filename;
- require unique trusted static.okx.com URL;
- HEAD only.

No product, family, hedge, contract, source-role or economic rules change.

## 6. Firewalls

Still forbidden:

- archive body GET/open;
- price;
- basis;
- settlePx;
- delivery price;
- convergence;
- strategy signal;
- execution/PnL;
- candidate-ID assignment.
