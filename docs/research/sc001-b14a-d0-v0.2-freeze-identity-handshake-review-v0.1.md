# SC001 — B14-A D0 v0.2 Freeze Identity Handshake Review v0.1

Date: 2026-09-19
Status: **ENGINEERING HANDSHAKE REVIEW / NO SOURCE OR PRICE OUTCOME**
Scope: `SCALPING RESEARCH / SC001`

## 1. Observed state

B14-A D0 v0.2 self-test returned:

`B14A_D0_V02_SELF_TEST_REVIEW`

Error:

`freeze identity mismatch: source_audit_git_blob_sha`

## 2. Root cause

The v0.2 implementation freeze correctly stores the product-family amendment parent as:

- `source_review_path`;
- `source_review_git_blob_sha`.

The v0.2 runner instead still checks the stale key:

`source_audit_git_blob_sha`

against the original v0.1 source/spec audit.

Therefore the runner/freeze handshake schema is inconsistent.

Classification:

`ENGINEERING_PARENT_IDENTITY_KEY_MISMATCH`

## 3. Research implication

The self-test stopped before any OKX API/source call.

Therefore:

- no FUTURES metadata outcome was opened;
- no archive metadata was queried by this run;
- no trade body was opened;
- no price/basis/settlePx/deliveryPx/convergence/PnL was accessed.

## 4. Allowed repair

Create v0.3 changing only:

- implementation version/stage labels;
- freeze/output path;
- parent identity check from stale `source_audit_git_blob_sha` to the correct `source_review_git_blob_sha`;
- parent path to the product-family source-review artifact.

No product family, contract semantics, hedge identity, D0 gates or firewalls change.

## 5. No-change list

Still frozen:

- BTC-USD / ETH-USD inverse standard expiry futures;
- BTC-USD-SWAP / ETH-USD-SWAP hedges;
- metadata-only D0;
- >=2 future contracts per family;
- >=1 FUTURES archive metadata PASS per family;
- no price outcomes.
