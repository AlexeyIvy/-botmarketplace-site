// /lab/data — Data tab placeholder (Phase 2 will add Dataset Builder)
export default function LabDataPage() {
  return (
    <div style={{ padding: "48px 40px" }}>
      <h2 style={{ fontSize: 18, marginBottom: 12, color: "var(--text-primary)" }}>
        Data
      </h2>
      <p style={{ color: "var(--text-secondary)", fontSize: 14, lineHeight: 1.6 }}>
        Dataset builder coming in Phase 2. You will be able to connect an
        exchange, select instruments, define timeframes and date ranges, and
        build reusable market datasets.
      </p>
    </div>
  );
}
