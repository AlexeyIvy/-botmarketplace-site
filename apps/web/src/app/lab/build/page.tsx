// /lab/build — Build tab placeholder (Phase 3 will add Strategy Canvas)
export default function LabBuildPage() {
  return (
    <div style={{ padding: "48px 40px" }}>
      <h2 style={{ fontSize: 18, marginBottom: 12, color: "var(--text-primary)" }}>
        Build
      </h2>
      <p style={{ color: "var(--text-secondary)", fontSize: 14, lineHeight: 1.6 }}>
        Strategy canvas coming in Phase 3. You will be able to visually compose
        strategies from typed building blocks using a node-based editor.
      </p>
    </div>
  );
}
